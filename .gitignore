build/viz-data.json
build/china-day-load.html
build/gmaps-key.txt

# editor/agent scratch copies — never part of the build
*.bak
*.bak2
.vercel

# build tooling only — the page itself has no dependencies
node_modules/
package-lock.json
